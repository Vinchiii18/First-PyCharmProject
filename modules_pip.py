# pip is like a npm, it lets you download and install other
# libraries that are not included in the python library
# just open CMD and install ones that you like
# e.g. pip install python-docx
# to uninstall - pip uninstall python-docx

import useful_tools
print(useful_tools.roll_dice(3))
